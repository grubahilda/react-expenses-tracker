import { useState } from 'react';

const Modal = (props: any) => {
  

  const onCloseButton = () => {
    props.onCloseButton();
  };

  return (
    <div>
      
    </div>
  );
};

export default Modal;
